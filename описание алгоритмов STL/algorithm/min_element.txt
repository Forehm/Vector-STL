
min_element(начало массива, конец массива)

выдает минимальное значение;

auto result = min_element(arr.begin(), arr.end());

при выводе result его нужно разыменовать.

При собственных типах данных у них должен быть перегружен оператор сравнения