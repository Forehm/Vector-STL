то же, что и unique, но третим параметром может принять back_inserter()
Если в этот метод (back_inserter()) мы передадим название другого контейнера,
то unique_copy() скопирует в него повторяющиеся элементы