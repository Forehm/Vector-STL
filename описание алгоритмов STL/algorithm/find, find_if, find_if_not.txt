Этот алгоритм ищет элементы в коллекции.
find(v.begin(), v.end(), 4)
принимает начало коллекции, конец и то, что мы ищем. В данном случае число 4.

У него есть разновидность find_if, третьим параметром он будет принимать предикат.

find_if_not это инвертированный find_if